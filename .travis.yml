language: php
php:
  - 8.0
  - 8.1
  - 8.2
env: PHPV=0
install: |
  composer install
script: |
  composer run-script test -- --colors --coverage-text
cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.titi
    - vendor
